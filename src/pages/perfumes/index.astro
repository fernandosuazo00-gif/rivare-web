---
import Base from '@/layouts/Base.astro';
import CollectionView from '@/components/CollectionView.astro';
import { products } from '@/data/products';

const itemListLd = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Perfumes RIVÂRE',
  numberOfItems: products.length,
  itemListElement: products.map((p, i) => ({
    '@type': 'ListItem',
    position: i + 1,
    url: new URL(`/perfume/${p.slug}`, Astro.site).href,
    name: `${p.name} — inspirado en ${p.inspiredBy}`,
  })),
};
---

<Base
  title="Perfumes"
  description="Las 17 fragancias RIVÂRE en Eau de Parfum, 50 ml. Perfumes inspirados en los grandes, hechos en Honduras — para hombre, para mujer y unisex."
  jsonLd={itemListLd}
>
  <CollectionView
    active="all"
    eyebrow={`Ver todo · ${products.length} fragancias`}
    title="Toda la colección"
    intro="Eau de Parfum de 50 ml, cada uno inspirado en un aroma reconocido en el mundo e interpretado por RIVÂRE. Filtrá por bestsellers, novedades, hombre, mujer o unisex."
    products={products}
  />
</Base>
